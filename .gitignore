# 构建 / 部署产物（不进代码仓库；tvbox-dist 由云端推到独立的 Pages 仓库）
tvbox-dist/
dist/
build/
*.exe
__pycache__/
*.pyc

# 本地虚拟环境
.venv/
venv/
envs/

# 采集产生的图片缓存（image_cache 不进仓库）
backend/data/image_cache/
# 本地运行态片库（每次采集重写，不进仓库；2026-08-19）
backend/data/db.json
# 本地运行配置（含机器相关运行态字段，不进仓库；2026-08-19）
backend/data/config.json

# 导出/构建临时产物（output/images 是自建海报仓库，必须进仓库；export_test 为测试产物，不进仓库）
output/json/
output/backup/
output/posters/
output/export_test/
node_modules/
*.zip

# 日志
*.log
.tmp_apk_extract/

# 本地部署凭据（已确认泄露，禁止入库；2026-08-18 安全修复）
backend/data/.fc_deploy_key

# Python 缓存 / 测试缓存
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/

# 编辑器 / 操作系统临时文件
.DS_Store
Thumbs.db
*.swp
*.swo
*.bak
*.tmp
.idea/
.vscode/

# 运行时状态 / 本地备份（不进仓库）
backend/data/run_status.json
backend/data/config.json.bak
# 跨进程运行锁目录与每日运营报告（运行态，不进仓库；2026-08-19）
output/.locks/
output/report_daily.json
